{
  "name": "portal-plugins-public",
  "description": "Plugins for cantemo portal",
  "version": "1.0.0",
  "devDependencies": {
    "jshint": "^2.9.4"
  },
  "scripts": {
    "test": "find gnm{awsgr,gridintegration} -iname '*.js' | grep -v '^./static' | grep -v node_modules | xargs ./node_modules/.bin/jshint"
  }
}